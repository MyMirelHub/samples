apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: appconfig
spec:
  metrics:
    enabled: true
    http:
      pathMatching:
        - /orders/{orderID}
      increasedCardinality: true